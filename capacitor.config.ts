import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mharsmc.ipcguard',
  appName: 'MHARSMC IPC Guard',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
